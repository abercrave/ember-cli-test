import {
  trimWhitespace
} from 'ember-command-line/helpers/trim-whitespace';

module('TrimWhitespaceHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = trimWhitespace(42);
  ok(result);
});
